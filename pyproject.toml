[project]
name = "ownai"
version = "0.1.0"
description = "OwnAI Agent Framework"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "loguru>=0.7.3",
    "requests>=2.32.5",
]

[tool.hatch.build.targets.wheel]
packages = []
