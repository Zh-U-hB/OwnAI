# 开发任务计划

## 优先级：🔴 高 - Agent与大模型对话模块

### 任务1：设计并实现LLM通信接口

- [ ] **1.1** 创建LLM客户端抽象类 src/agent/llm_client.py
  - [ ] 定义LLMProvider接口（支持OpenAI、Anthropic，以及自定义模型等）
  - [ ] 实现统一的请求/响应格式
- [ ] **1.2** 配置管理 src/agent/config.py
  - [ ] 创建API密钥配置模块
  - [ ] 支持多模型切换（可以给不同Agent指定不同模型，用户可以指定默认模型）

### 发送的messages需要在发送前进行格式化

保证messages的格式可以嵌入多重Prompt，例如system prompt、user prompt、assistant prompt等。

### LLM接口格式

代码需要从环境变量中读取API密钥和模型名称，而不是直接在代码中写死。

```python
def call_llm(self, url, api_key, model, messages) -> str:
    """
    调用大模型API
    :param url: API URL
    :param api_key: API密钥
    :param model: 模型名称
    :param messages: 消息列表
    :return: 模型响应
    """
```
# 11111111111

### 密钥配置格式

```env
# 基础配置(自定义API)
LLM_URL = LLM_API_URL
LLM_API_KEY = Your_API_Key
Model = LLM_Model_Name

# 预设大模型供应商（已有大模型供应商）

## OpenAI
OpenAI = OpenAI_API_URL
OpenAI_API_Key = Your_OpenAI_API_Key
OpenAI_Model = OpenAI_Model_Name

## Anthropic
Anthropic = Anthropic_API_URL
Anthropic_API_Key = Your_Anthropic_API_Key
Anthropic_Model = Anthropic_Model_Name

## Gemini
Gemini = Gemini_API_URL
Gemini_API_Key = Your_Gemini_API_Key
Gemini_Model = Gemini_Model_Name

## embedding模型
Embedding_URL = Embedding_API_URL
Embedding_API_Key = Your_Embedding_API_Key
Embedding_Model = Embedding_Model_Name
......

```

### 密钥配置说明

- test
